#!/bin/sh
set -e
apt-get purge -yy @PVER@ || true
MODS="_ssl _hashlib _fileio _bytesio"
for mod in $MODS; do
  @PVER@ -c "import $mod"
done

test ! -e /usr/lib/@PVER@/lib-dynload/_fileio.so
test ! -e /usr/lib/@PVER@/lib-dynload/_bytesio.so
